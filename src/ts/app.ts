import * as dbConnect from './db';
// import * as auth from './auth';

console.log("App started..");


// import * as httpServer from 'http-server';
// const serverOptions: httpServer.Options = {
//   root: "./",
//   showDir: true
// };

// httpServer.createServer(serverOptions);
